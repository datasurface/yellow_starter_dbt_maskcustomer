name: 'masked_customers_dbt'
version: '1.0.0'
config-version: 2

# This profile will be generated by DataSurface from profiles.yml.j2
profile: 'masked_customers_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

